
// SERVICE ROUTES
// Routes: GET /, GET /:id, POST / (admin), PUT /:id (admin), DELETE /:id (admin)
// Developer: [ASSIGNED_DEV]
const express = require('express');
const router = express.Router();

module.exports = router;

